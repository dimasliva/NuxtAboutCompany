export interface IContactBlock {
    title: string;
    textOne: string;
    textTwo: string;
}
export interface IFormDataType {
    firstName: string;
    lastName: string;
    email: string;
    company?: string; 
    message: string;
}